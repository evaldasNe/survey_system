<ul class="nav bg-dark justify-content-end">
    <li class="nav-item active">
        <a class="nav-link" href="{{ absolute_url(path('home_page')) }}">Home page</a>
    </li>
    {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item active">
            <a class="nav-link" href="{{ absolute_url(path('author_index')) }}">Authors</a>
        </li>
    {% endif %}
    {% if is_granted('ROLE_AUTHOR') %}
        <li class="nav-item active">
            <a class="nav-link" href="{{ absolute_url(path('survey_index')) }}">My surveys</a>
        </li>
    {% endif %}
    {% if app.user %}
        <li class="nav-item active">
            <a class="nav-link text-danger" href="{{ absolute_url(path('app_logout')) }}">Logout</a>
        </li>
        {% else %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ absolute_url(path('app_login')) }}">Login</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ absolute_url(path('app_register')) }}">Sign up</a>
            </li>
    {% endif %}
</ul>
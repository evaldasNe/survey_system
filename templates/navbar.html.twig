<ul class="nav bg-dark justify-content-end">
    {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item active">
            <a class="nav-link" href="{{ absolute_url(path('author_index')) }}">Autoriai</a>
        </li>
    {% endif %}
    {% if app.user %}
        <li class="nav-item active">
            <a class="nav-link text-danger" href="{{ absolute_url(path('app_logout')) }}">Atsijungti</a>
        </li>
        {% else %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ absolute_url(path('app_login')) }}">Prisijungti</a>
            </li>
    {% endif %}
</ul>
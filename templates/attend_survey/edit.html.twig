{% extends 'base.html.twig' %}

{% block title %}Edit AttendSurvey{% endblock %}

{% block body %}
    <h1>Edit AttendSurvey</h1>

    {{ include('attend_survey/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('attend_survey_index') }}">back to list</a>

    {{ include('attend_survey/_delete_form.html.twig') }}
{% endblock %}

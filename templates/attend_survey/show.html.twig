{% extends 'base.html.twig' %}

{% block title %}AttendSurvey{% endblock %}

{% block body %}
    <h1>AttendSurvey</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attend_survey.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attend_survey_index') }}">back to list</a>

    <a href="{{ path('attend_survey_edit', {'id': attend_survey.id}) }}">edit</a>

    {{ include('attend_survey/_delete_form.html.twig') }}
{% endblock %}

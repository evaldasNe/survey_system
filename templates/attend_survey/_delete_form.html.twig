<form method="post" action="{{ path('attend_survey_delete', {'id': attend_survey.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attend_survey.id) }}">
    <button class="btn">Delete</button>
</form>
